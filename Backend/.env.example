ACCESS_TOKEN_SECRET=your_access_token_secret_here
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here

RESEND_API_KEY=
VITE_EXCHANGE_RATE_API_KEY=
GEMINI_API_KEY =

# 🧠 Claude "brain" — reliable intent understanding (any phrasing, no regex).
# Paste your key here → smart mode ON. Leave blank → app runs deterministic (as before).
# Get a key + add $5 credit at https://console.anthropic.com (Settings → API Keys / Billing).
ANTHROPIC_API_KEY=
# Swap the model without touching code (default: claude-haiku-4-5 — fast + cheap).
AI_MODEL=claude-haiku-4-5
# Optional kill-switch: set to "off" to force deterministic mode even with a key set.
AI_BRAIN=
